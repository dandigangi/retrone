import type { NextPage } from 'next';
import PageBoard from '../modules/pages/Board';

/*
 * @Page Board
 * @Route /board/{id}
 */
const Board: NextPage = () => {
    return <PageBoard />;
};

export default Board;
